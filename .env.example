# Postgres connection string (Railway, Neon, Supabase, local...)
DATABASE_URL="postgresql://user:password@localhost:5432/mosaic"

# Google Gemini — powers every creative-analysis call
GEMINI_API_KEY=""
GEMINI_MODEL="gemini-2.0-flash"

# Apify — scrapes video stats and the media file itself
APIFY_TOKEN=""
APIFY_TIKTOK_ACTOR="clockworks~tiktok-scraper"
APIFY_INSTAGRAM_ACTOR="apify~instagram-reel-scraper"

# Shared secret for the background job endpoints (cron / queue worker)
JOB_SECRET=""
